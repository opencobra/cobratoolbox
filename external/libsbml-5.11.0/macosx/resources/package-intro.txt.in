Welcome to the installation program for libSBML @PACKAGE_VERSION@ (configured to use the @XML_PARSER@ parser). In the following panels, you will be guided through the steps necessary to install the libSBML library and the language interfaces on your system.
