function [r_info]=correct_M(ref,listN,listR)

% Correct(Update) the species name according to a reference list of the names.
%
%INPUTS
%
% ref           A parsed CD model structure generated by 'parseCD'function.
% listN         A matlab variable that saves the lines of the XML file.
% listR         A list of reaction IFD 
% 
%OPTIONAL INPUT
%
% note          The default value is '0'; It can be set as 'true';
%               Record the column number of the species that are to be
%               replaced with a new name.
%
%OUTPUTS
%
% text            The corrected lines of the XML file.
% number          The number of the line.
% changdStrLines  The changed line of text.
% col             Record the column number of the species that is to be
%                 replaced with a new species name.
%
%
% EXAMPLE
% 
% ref_corrected=correct_M(ref,cmp_recon2map_M.listOfNotFound(:,6),listForCorrection(:,2))
%
% Longfei Mao Oct/2015

col_name=2;


r_info=ref.r_info;
r_listN=listN
r_listR=listR


for n=1:length(r_listR)
    r_info.species(r_listN{n},col_name)=r_listR(n);
end


