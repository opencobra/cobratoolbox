# Baseline compliance report (preliminary)

**Status**: PRELIMINARY heuristic pre-scan captured at planning time. The **authoritative**
baseline is regenerated by `checkFunctionHeaders.m` / `testHeaderCompliance.m` as the
first implementation task (FR-002) and will replace the numbers below with per-rule
counts. This preliminary snapshot exists to size the work and drive `tasks.md`.

**Scan date**: 2026-07-17 (branch `014-src-header-compliance`, before remediation)

## Scope

- Total `src/*.m`: **1578** (1511 functions, 67 scripts)
- **In scope**: **1498** files across **241** leaf folders
- **Excluded (vendored)**: **80** files
  - `src/reconstruction/rBioNet/**` — 40 files
  - `src/reconstruction/comparison/modelBorgifier/**` — 39 files
  - `src/base/io/python/pyPath/py_addpath.m` — 1 file (file-level licence guard)

## Per-domain in-scope inventory

| Domain | In-scope files | Leaf folders |
|--------|----------------|--------------|
| analysis | 605 | 117 |
| base | 238 | 42 |
| dataIntegration | 248 | 39 |
| design | 60 | 5 |
| reconstruction | 237 | 23 |
| visualization | 110 | 15 |
| **total** | **1498** | **241** |

Full per-folder inventory: [`inscope-folders.tsv`](./inscope-folders.tsv).

## Heuristic compliance gaps (all `src/*.m`, keyword-presence only)

These are coarse presence checks (not the full rule catalog), indicative of the work:

| Signal | Count | % of functions |
|--------|-------|----------------|
| functions with a `USAGE:` block | 1085 | 71% |
| functions with an `INPUT(S):` block | 1049 | 69% |
| functions with an `OUTPUT(S):` block | 938 | 62% |
| functions documenting **neither** input nor output | 426 | 28% |
| function files with a **completely empty** header | 85 | — |
| header(s) with ≥1 `%text` (missing-space) line | 68 | — |

The authoritative baseline will additionally quantify H-ARGFMT, H-FIELD, H-FIELDUSE,
H-NV, H-SIG, and H-BODYGAP violations, which this heuristic does not measure.
